# 开发路线与验收范围

## 当前生产（2026-09-25）

用户于 2026-09-25 确认已手动部署阶段 1–3；生产应已应用迁移至 011。该生产状态来自用户确认，本工作区未重新连接生产检查。基础版本包含五模块前台、同页上传、访客下载及 MIDI / 人物删除；旧发布细节见 [验证报告](implementation-report.md)。

- MIDI 与人物编辑页增加独立删除确认、取消及提交中禁用；旧版本拒绝删除，响应丢失后可核对列表并安全处理已删除结果。
- 人物仍被作品署名或寻回记录引用时拒绝删除，必须先手动解除引用；不静默抹去历史关联。
- MIDI 删除级联所属署名、来源、寻回和文件登记，不删除关联人物；外部对象写入 journal，至少 24 小时后才可由显式维护清理，不自动清理或修改桶权限。
- 008 保留已删除档案的创建回执，旧请求重放返回 `410 CREATION_DELETED`；并发文件锁冲突返回 `503 SERVER_BUSY`，避免锁顺序死锁。
- 本地后端 101 项通过、2 项 Windows 符号链接测试跳过；迁移重复执行、HTTP、桌面/手机浏览器及禁用导入回归通过。线上只读验收未试删资料；本机后端健康端点直连超时仍是验证限制。

## 已完成：阶段 1–3

这些阶段已于 2026-09-25 手动部署（按用户确认）。新代码要求数据库至少迁移至 011。

- 阶段 1：Sitemap 根据总数分页读取 MIDI 与人物，分页请求每批最多并发 4 个；增加只面向空隔离数据库的恢复演练脚本，并将 CI smoke 覆盖目录搜索、Sitemap 和 robots 声明。
- 阶段 2：迁移 009 安装 PostgreSQL `pg_trgm` 索引；公开搜索可匹配作品标题、slug、人物姓名、历史昵称和历史来源，支持分页。搜索结果页设置 `noindex`。
- 阶段 3：迁移 010 为 MIDI 和人物增加软删除、回收站与事务内审计；管理员可恢复无损保留的记录。迁移 011 为孤儿对象清理 journal 记录重试次数和通用错误码，失败后保留 journal 供后续显式重试。
- 真实数据库恢复演练、自动化备份告警、实际 S3 备份与永久删除策略仍待运营层完善。

## 已实现：阶段 4–6（本分支）

- 阶段 4：来源类型与人工可信度评级、最后核验时间、历史快照及小型证据附件；附件受管理端鉴权并保存 SHA-256。
- 阶段 5：用户身份分为超级管理员、管理员和访问者；前两类通过邀请加入，管理员变更走审核，超级管理员管理账号与发布权限，访问者只读浏览且不需要注册。
- 阶段 6：为作品与人物增加稳定 UUID 公开标识，兼容旧编号路由；升级 API 与数据库索引，保持现有 slug 链接，并逐步整理语言策略。

当前落地边界：阶段 4 支持来源可信度、核验时间与小型证据附件；尚无自动网页抓取或 Wayback 快照生成。阶段 5 支持多位超级管理员/管理员、邀请、角色管理与审核；管理员可提交所有内容写操作，包括基础资料、署名、历史来源/寻回、证据、文件导入、删除和恢复；超级管理员审核执行。匿名访问者只读。阶段 6 的公开详情与站点地图使用稳定 UUID，旧作品 slug 与人物数字编号兼容重定向；语言策略整理仍待后续。

## 已实现

- 基础站点、公开 MIDI 与人物详情、来源及寻回记录展示、数据库迁移和 Docker Compose 部署。
- 统一后台、多管理员认证与邀请、会话撤销和版本冲突保护；管理员所有内容写操作均经超级管理员审核。
- 人物资料与历史昵称新增、编辑；作品署名的添加、角色调整与移除。超级管理员保存立即公开，管理员提交审核，人物不是登录账号。
- 历史来源与寻回记录逐条新增、编辑、删除，保留记录编号和寻回记录创建时间；超级管理员保存立即同步公开详情。
- 来源网址、完整 UTC 时间和人物关联校验；未知日期及人物使用空值，不自动更改归档状态或署名。
- 前端在同一 Git 仓库内独立安装、构建和部署，可用 Vercel；后端及数据库单独运行。
- 一次性 `/install`：后端安装令牌授权、站点名称与简介、数据库单管理员及持久安装锁，兼容已有环境管理员；前端仅生成三项连接/来源/Cookie 变量文本，Vercel 用户自行保存项目变量并重部署，不自动写 env 或调用 Vercel API。已通过本地安装、旧环境升级兼容及故障处理回归；部署验证范围见下文。

## 前后台改版（2026-09-24）

- 前台提供首页总览、MIDI、作者、寻回进度和 Map；关于项目移至页脚。列表支持分页、状态筛选和排序，Map 按署名人物或历史来源分组，并包含未署名、来源待补组。
- 新增公开目录 API：`/api/v1/catalog/overview`、`/api/v1/catalog/entries`、`/api/v1/catalog/groups` 和 `/api/v1/people`。统计区分作品、文件、有文件作品和符合下载权限作品；SQL 批量补充署名与来源，分页与统计使用同一只读快照。
- 后台移除“管理功能”、服务连接和工作模式展示，工作台提供待完善记录、最近修改与快捷新增。
- 新建档案可同页选择一个 MIDI 文件、确认公开分发并一次保存；仍支持仅创建资料。关闭导入时隐藏文件输入并保留资料建档。
- `007_midi_creation_requests.sql` 新增请求回执。档案、可选文件登记和回执同事务提交；失败不留下半成品档案。相同请求安全重试，响应丢失时保留表单与文件，锁定本次内容后重试；内容变化的同一请求返回冲突。
- 上述旧版功能已包含在当前生产；当前阶段 4–6 分支要求应用全部迁移至 015，再更新后端与前端，保留安装锁、现有记录与文件。

该阶段本地验收：后端 98 项通过、2 项 Windows 符号链接权限相关跳过；前端 lint、类型检查、生产构建，以及隔离 HTTP/桌面与手机浏览器回归通过。历史记录见 [2026-09-24 本地验收记录](releases/2026-09-24-catalog.md)，最新删除功能与生产发布结果见本文开头。

## 管理员 MIDI 导入与访客下载

- 公开详情页提供 MIDI 下载按钮、加载与失败提示，保留原始文件名；匿名下载经站内接口读取 local / S3，校验大小、SHA-256、文件归属与分发确认。`restricted` / `metadata_only` 条目不开放站内下载；无文件及旧未确认记录保持元数据展示。当前生产已提供访客下载，本次读取现有文件并核对大小及 SHA-256 通过。

- 已有管理员单文件导入实现：SMF 0/1/2、单个 `.mid` / `.midi`、最大 1 MiB，须确认有权公开分发；默认 `MIDI_IMPORT_ENABLED=false`。对象存入 S3 并允许匿名读取，不提供后台对象 URL 或下载试听，不自动修改版权、分发许可或归档状态。
- `GET/POST /api/v1/admin/midis/{id}/files` 使用管理员 Bearer；相同档案相同 SHA-256 幂等，跨档案返回 `409 FILE_OWNERSHIP_CONFLICT`，新文件登记与父 revision 递增原子提交。HTTP 使用 `MidiImportService`，不是旧内部 `MidiFileService`。
- 已接入 local / S3 存储选择、先写 journal 再写对象及同 digest advisory lock。显式 `lostmidi_api --cleanup-imports` 仅处理超过 24 小时且无引用的 journal，每次最多 100 条，不列桶、不扫目录；必须匹配原数据库与存储 backend/bucket/prefix，不能拿生产执行做测试。
- **本地验证通过**：50 项后端测试，含隔离 PostgreSQL 的幂等、归属冲突、连续并发重试、SQL/提交失败回滚及安全清理；迁移重复执行、HTTP 导入/禁用检查、前端 lint/类型检查/生产构建、浏览器文件保留与版本冲突恢复、桌面/手机布局均通过。
- **真实桶联调通过**：对雨云 ROS（Ceph RGW）桶验证签名读写、条件 PUT（`412`）、Range GET 与 DELETE；期间定位并修复了 PUT 必须把 `Content-Type` 纳入签名头（否则返回 `403 AccessDenied`）的兼容性问题。`S3_PUBLIC_DISTRIBUTION_CONFIRMED=true` 只是人工确认，不校验权限；对象公开读取由桶策略决定。上线前须撤销并更换已暴露的旧密钥，不复用。

来源与寻回仍只维护已掌握的历史信息；没有自动抓取 URL 或生成 Wayback capture。匿名访问者无账号且只读；管理员可提交所有内容变更申请，由超级管理员审核执行。全站语言策略尚未统一。

## 计划中（尚未实现）

- **统一语言策略**：目前页面以中文为主，文档含中英文混排，后续再统一导航、元数据与内容编辑的语言策略。


## 历史部署与验证（006）

以下保留 006 阶段的交付边界，不表示当前生产版本；当前生产已至 008，见本文开头。当时生产已运行于独立 Vercel 后端容器项目 + Neon Free，数据库已迁移到 006，相应代码已发布。新版启动及 `/ready` 会检查 006 journal 和分发确认字段（列名保留 `private_archive_confirmed`，现记录公开分发确认），**即使关闭导入也须先迁移**。已有库先备份，使用现有幂等 `database/migrate.sh` 执行全部待应用迁移至 006，再更新应用；不要修改旧迁移或使用要求空库的 `.tools` 临时脚本。005 安装锁与已有数据保留；旧环境管理员首次升级时仍须保留完整凭据直到 legacy 标记持久化，见 [RUN.md](../RUN.md)。

本轮已完成本地代码、配置与上述验证，并对真实雨云 ROS 桶联调验证签名读写、云构建与部署上线（默认关闭导入）。前端继续使用 Root Directory=`frontend`，后端使用根 `vercel.json`，旧 VPS / Compose 方案仍可用。保持 2 个连接 / 2 个 worker、Preview 隔离和少量按需验收，避免反复云构建消耗额度。

安装不创建数据库、不自动迁移或 seed，不提供重装/reset、设置编辑或密码重置页。安装功能边界见 [Admin 文档](admin.md)，历史本地验收记录见 [Implementation Report](implementation-report.md)，不是本轮导入验收证明。自动化写入检查只在专用测试库执行；两种安装 smoke 必须分别使用全新、已迁移的独立专用测试库，会永久安装，不能顺序指向同库。
