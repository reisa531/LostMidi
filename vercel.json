{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["iad1"],
  "services": {
    "backend": {
      "root": ".",
      "runtime": "container",
      "entrypoint": "docker/Dockerfile.backend"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": { "service": "backend" } }
  ]
}
