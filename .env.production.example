# Production template: fill all empty credentials and URLs before starting Compose.
POSTGRES_USER=lostmidi
POSTGRES_PASSWORD=
POSTGRES_DB=lostmidi
POSTGRES_PORT=5432
DATABASE_URL=
BACKEND_API_URL=http://backend:8080
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8080
FRONTEND_PORT=3000
DB_POOL_SIZE=4
HTTP_THREADS=2
WORKER_THREADS=4
STORAGE_PATH=./storage
SEED_DEMO=false
ADMIN_USERNAME=admin
# Generate using python scripts/admin_password.py; never enter a plaintext password.
ADMIN_PASSWORD_HASH=
# Your HTTPS browser origin, without a trailing slash.
ADMIN_ORIGIN=
ADMIN_COOKIE_SECURE=true
